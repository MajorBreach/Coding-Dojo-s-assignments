class BankAccount:

    def __init__(self, int_rate, balance):
        self.int_rate = int_rate
        self.balance = balance
        amount = float
        print(self)
# methods

    def deposit(self, amount):
        amount = float
        self.balance = amount
        print(amount)

    def withdraw(self):

        if self.balance >= amount:
            self.balance -= amount
            print(self.amount)

    # def display_account_info(self):
    #     print(self.balace)

    def yield_interest(self):
        self.int_rate = .01
        print(self)


# Execute's
Ranked = BankAccount(.01, 0)
Ranked.deposit()
Ranked.withdraw()
Ranked.yield_interest()
